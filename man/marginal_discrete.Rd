% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_discrete.R
\name{marginal_discrete}
\alias{marginal_discrete}
\title{Marginal distribution table for a discrete variable}
\usage{
marginal_discrete(data, var_name)
}
\arguments{
\item{data}{A data frame.}

\item{var_name}{Character. Name of the column in \code{data} to summarise. Used
as the header of the category column. The column may be any type
(character, factor, logical, etc.). \code{NA} values are always included as
their own category. The \code{NA} row is never collapsed into \code{"< 5\%"}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{table}}{A \code{knitr_kable} markdown table with columns
\code{Category}, \code{n} (comma-formatted), and \verb{\%} (1 decimal place).
Rows are sorted descending by count.}
\item{\code{table_collapsed}}{A \code{knitr_kable} markdown table in the same
format, with small categories collapsed into \code{"< 5\%"}. \code{NULL} when
there are 10 or fewer categories, or when no category individually
falls below 5\%.}
\item{\code{.var_name}}{Character. The variable name used for file naming in
\code{\link[=save_summaries]{save_summaries()}}.}
\item{\code{.summary_type}}{Character \code{"discrete_marginal"}. Used by
\code{\link[=save_summaries]{save_summaries()}} to dispatch saving logic.}
}
}
\description{
Produces a frequency table for a discrete (categorical) variable showing
counts and percentages. When there are more than 10 categories, a second
collapsed table is also returned in which the smallest categories — those
that each individually represent less than 5\% of observations — are
combined into a single \code{"< 5\%"} row.
}
\details{
The collapsed table uses the greatest number of categories for which every
individual collapsed category is below 5\%. Categories are evaluated in
ascending order of frequency; collapsing stops as soon as a category
reaches or exceeds 5\%.
}
\examples{
# Few categories — no collapsed table
df <- data.frame(letter = sample(letters[1:5], 100, replace = TRUE))
out <- marginal_discrete(df, "letter")
out$table
out$table_collapsed  # NULL

# Many categories — collapsed table produced
df2 <- data.frame(letter = sample(letters, 500, replace = TRUE))
out2 <- marginal_discrete(df2, "letter")
out2$table
out2$table_collapsed

}
