% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim.R
\name{trim_count}
\alias{trim_count}
\title{Trim the n smallest and n largest observations}
\usage{
trim_count(n = 10L)
}
\arguments{
\item{n}{Integer. Number of observations to remove from each tail. Default
\code{10L}.}
}
\value{
A function that accepts a numeric vector and returns an integer
vector of the same length with \code{0} for values to keep and \code{1} for values
to trim.
}
\description{
Returns a trimming function that marks the \code{n} smallest and \code{n} largest
values in a numeric vector as trimmed. Designed to be passed to the \code{trim}
argument of continuous summary functions (e.g. \code{\link[=marginal_continuous]{marginal_continuous()}}).
}
\details{
Like \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}}, calling \code{trim_count()} returns a
function; that function is what the summary functions invoke on the data.
}
\examples{
f <- trim_count(5)
f(1:20)  # positions 1-5 and 16-20 get 1

df <- data.frame(x = rnorm(200))
out <- marginal_continuous(df, "x", trim = trim_count(10))

}
