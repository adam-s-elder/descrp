% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_summaries.R
\name{save_summaries}
\alias{save_summaries}
\title{Save all summary objects produced by descrp functions}
\usage{
save_summaries(summaries, output_dir = ".", width = 8, height = 6, dpi = 150)
}
\arguments{
\item{summaries}{A single summary list returned by a descrp function, \strong{or}
a plain list whose elements are such summary lists.}

\item{output_dir}{Character. Directory in which to write files. Created
(recursively) if it does not already exist. Defaults to \code{"."}.}

\item{width, height}{Numeric. Width and height in inches passed to
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} for PNG files. Defaults: \code{8} and \code{6}. For
\code{continuous_discrete_joint} summaries the height is overridden by the
\code{.plot_height} field embedded in the summary object when it is larger than
\code{height}.}

\item{dpi}{Numeric. Resolution passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. Default \code{150}.}
}
\value{
Invisibly returns a character vector of the file paths written.
}
\description{
Writes every non-\code{NULL} plot or table from the \code{outputs} element of one or
more summary lists (as returned by any descrp summary function) to files in
\code{output_dir}. File names encode both the variable name(s) and the kind of
summary, e.g. \code{score_marginal_hist.png} or \code{group_table_collapsed.md}.
}
\details{
ggplot2 objects are saved as PNG; \code{knitr_kable} tables are saved as \code{.md}.
\code{NULL} outputs are silently skipped.
}
\examples{
\dontrun{
df <- data.frame(
  score  = rnorm(300),
  group  = sample(c("A", "B", "C"), 300, replace = TRUE),
  score2 = rnorm(300)
)
summaries <- list(
  marginal_continuous(df, "score"),
  marginal_discrete(df, "group"),
  joint_continuous_continuous(df, "score", "score2"),
  joint_continuous_discrete(df, "score", "group"),
  joint_discrete_discrete(df, "group", "group")
)
save_summaries(summaries, output_dir = "output")
}

}
