% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_discrete_discrete.R
\name{joint_discrete_discrete}
\alias{joint_discrete_discrete}
\title{Cross-tabulation of two discrete variables}
\usage{
joint_discrete_discrete(data, var1, var2)
}
\arguments{
\item{data}{A data frame.}

\item{var1}{Character. Name of the first discrete column.}

\item{var2}{Character. Name of the second discrete column.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{crosstab}}{A \code{knitr_kable} markdown table with \code{var1} levels as
rows, \code{var2} levels as columns, and a Total row and column. Each cell
shows \verb{count (row\%)}.}
\item{\code{crosstab_transposed}}{The same table with \code{var2} as rows and \code{var1}
as columns.}
\item{\code{.var_name}}{Combined variable name for file naming by
\code{\link[=save_summaries]{save_summaries()}}.}
\item{\code{.summary_type}}{Character \code{"discrete_discrete_joint"}.}
}
}
\description{
Produces two cross-tabulation tables showing the joint frequency
distribution of two categorical variables. Each cell contains the count and
the row percentage. \code{NA} values in either variable are included as their own
level.
}
\examples{
df <- data.frame(
  sex    = sample(c("M", "F"), 200, replace = TRUE),
  status = sample(c("Active", "Inactive", "Unknown"), 200, replace = TRUE)
)
out <- joint_discrete_discrete(df, "sex", "status")
out$crosstab
out$crosstab_transposed

}
