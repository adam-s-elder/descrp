% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trim.R
\name{trim_quantile}
\alias{trim_quantile}
\title{Trim values outside a quantile range}
\usage{
trim_quantile(pct = 0.01)
}
\arguments{
\item{pct}{Numeric in (0, 0.5). Tail fraction to remove from each end.
Default \code{0.01} (1\%).}
}
\value{
A function that accepts a numeric vector and returns an integer
vector of the same length with \code{0} for values to keep and \code{1} for values
to trim.
}
\description{
Returns a trimming function that marks values below the \code{pct} quantile or
above the \code{1 - pct} quantile as trimmed. Designed to be passed to the
\code{trim} argument of continuous summary functions (e.g.
\code{\link[=marginal_continuous]{marginal_continuous()}}).
}
\details{
Like \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}}, calling \code{trim_quantile()} returns a
function; that function is what the summary functions invoke on the data.
}
\examples{
f <- trim_quantile(0.05)
f(1:100)  # bottom 5 and top 5 get 1

df <- data.frame(x = rnorm(200))
out <- marginal_continuous(df, "x", trim = trim_quantile(0.01))

}
